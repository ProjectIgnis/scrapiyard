---!function
name: HasCounters
namespace: Card
description: >-
  Returns `true` if a given card currently holds any counter.
parameters:
  - name: c
    type: [ Card ]
    description: The card to check the counters of.
returns:
  - type: [ bool ]
    description: If `c` currently holds any counter.
status: { index: stable }
tags: [ card-check ]
suggestedLinks:
  - name: Card.HasCounter
    link: /api/functions/Card/HasCounter
    message: For checking if a card holds a specified counter
