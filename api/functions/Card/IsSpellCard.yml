---!function
name: IsSpellCard
namespace: Card
description: >-
  Returns `true` if the given card is originally a [Spell Card](https://yugipedia.com/wiki/Spell_Card).
summary: Checks if a card is originally a Spell Card
parameters:
  - name: c
    type: [ Card ]
    description: The card to check.
returns:
  - type: [ bool ]
    description: If `c` is originally a Spell Card.
status: { index: stable }
tags: [ card-check, card-type-related, original-property ]
