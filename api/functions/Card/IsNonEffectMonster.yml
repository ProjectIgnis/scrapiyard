---!function
name: IsNonEffectMonster
namespace: Card
description: >-
  Returns `true` if the given card is currently
  a [Non-Effect monster](https://yugipedia.com/wiki/Non-Effect_Monster).
summary: Checks if a card is a Non-Effect monster
parameters:
  - name: c
    type: [ Card ]
    description: The card to check.
returns:
  - type: [ bool ]
    description: If `c` is currently a Non-Effect monster.
status: { index: stable }
tags: [ card-check, card-type-related ]
