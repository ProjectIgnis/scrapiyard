---!function
name: GetFieldID
namespace: Card
description: >-
  Returns a card's current location ID, a value assigned to a card whenever it changes locations.
summary: Gets a card's current location ID
parameters:
  - name: c
    type: [ Card ]
    description: The card to get the current location ID of.
returns:
  - type: [ int ]
    description: The current location ID of `c`.
status: { index: stable }
suggestedLinks:
  - name: Card.GetRealFieldID
    link: /api/functions/Card/GetRealFieldID
    message: A function for getting a card's unique duel ID
