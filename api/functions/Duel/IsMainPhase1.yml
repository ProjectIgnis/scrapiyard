---!function
name: IsMainPhase1
namespace: Duel
description: >-
  Checks if it is currently the Main Phase 1. If `player` is provided it will also check that it is currently that player's turn.
status:
  index: stable
parameters:
  - name: player
    type: [ int ]
    description: (To be added)
    required: false
returns:
  - type: [ bool ]
    description: >-
      `true` if it is currently the Main Phase 1, `false` otherwise.
tags: [ under-construction ]
