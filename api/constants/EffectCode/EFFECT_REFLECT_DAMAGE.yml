---!constant
name: EFFECT_REFLECT_DAMAGE
enum: EffectCode
value: 83
description: If a player affected by an [effect](/api/types/Effect) with this [code](/api/functions/Effect/SetCode) would take damage that in a way that fulfills the effect's [value function](/api/functions/Effect/SetValue), the opponent takes the damage instead.
summary: Damage inflicted to affected players is inflicted to their opponent instead.
status:
  index: stable
suggestedLinks:
  - name: EFFECT_REFLECT_BATTLE_DAMAGE
    link: /api/constants/EffectCode/EFFECT_REFLECT_BATTLE_DAMAGE
# effect structure:
#   SetValue: function(e,amount,re,r,rp,rc) -> bool