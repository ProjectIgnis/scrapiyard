---!constant
name: EFFECT_IMMUNE_EFFECT
enum: EffectCode
value: 1
description: A card affected by an [effect](/api/types/Effect) with this [code](/api/functions/Effect/SetCode) is unaffected by effects that fulfill its [value function](/api/functions/Effect/SetValue).
summary: Opponent cannot attack affected monsters, but can attack directly.
status:
  index: stable
# effect structure:
#   SetValue: function(e,re,c) -> bool
#     checks if `re` can affect the handler of `e`
#     not certain what `c` is for