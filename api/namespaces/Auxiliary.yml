---!namespace
# `aux` is documented as the main name since it's what's used in most scripts,
# but this file's name as well as the corresponding function folder will be
# kept as `Auxiliary` because `aux` is a reserved name on Windows
name: aux
description: &desc Various helper functions.
summary: *desc
status:
  index: stable
aliases:
  - name: Auxiliary
    status:
      index: stable
